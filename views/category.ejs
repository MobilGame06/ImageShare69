<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Category</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-dark-5@1.1.3/dist/css/bootstrap-dark.min.css">
</head>

<body class="bg-dark">
  <a href="/">
    <img src="https://cdn-icons-png.flaticon.com/512/25/25694.png" alt="Logo" width="35" height="35" class="mx-auto d-block">
  </a>
  <h1 class="text-center text-white mt-5"><%= category %></h1>
  <div class="container mt-5">
    <div class="row">
      <% for (let i = 0; i < imageUrls.length; i++) { %>
        <div class="col-md-6 col-lg-4 mb-4">
          <div class="card">
            <img src="<%= imageUrls[i] %>" alt="Image" class="card-img-top" onclick="showImage(this)">
            <div class="card-body">
              <p class="card-text"><%= imageTexts[i] %></p>
            </div>
          </div>
        </div>
      <% } %>
    </div>
  </div>

  <div id="image-viewer" onclick="hideImage()">
    <img src="" alt="Full Image" id="full-image" class="rounded">
  </div>

  <style>
    body {
      font-family: 'Roboto', sans-serif;
    }

    .card {
      transition: transform 0.2s;
    }

    .card:hover {
      transform: scale(1.05);
    }

    #image-viewer {
      display: none;
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0, 0, 0, 0.7);
      z-index: 9999;
      text-align: center;
    }

    #image-viewer img {
      max-width: 90%;
      max-height: 90%;
      margin-top: 3%;
      animation-name: zoom;
      animation-duration: 0.5s;
      transform-style: preserve-3d;
    }

    @keyframes zoom {
      from {
        transform: scale(0);
      }

      to {
        transform: scale(1);
      }
    }
  </style>

  <script>
    function showImage(img) {
      var fullImg = document.getElementById("full-image");
      fullImg.src = img.src;
      document.getElementById("image-viewer").style.display = "block";
    }

    function hideImage() {
      document.getElementById("image-viewer").style.display = "none";
    }
  </script>
</body>

</html>
